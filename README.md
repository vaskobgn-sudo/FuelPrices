# Механата

Българска idle/tycoon игра за мобилен браузър. Наливаш ракия, купуваш постове,
трупаш звезди.

**Версия: Алфа 0.3**

## Как се играе

- **Онлайн:** https://vaskobgn-sudo.github.io/mehanata/ — от телефон:
  меню ⋮ → Споделяне → Добавяне към началния екран. Пуска се на цял екран
  и работи офлайн.
- **От файл:** свали `mehanata.html` и го отвори. Един файл, без интернет,
  без инсталация.

> При отваряне от диск на Android използвай Chrome и адрес `file://`
> (напиши го в адресната лента). През файлов браузър адресът става
> `content://`, а там браузърът не дава нито икона на началния екран, нито
> надеждна памет за записа. Играта го проверява при старт и казва, ако е така.

## Устройство на проекта

```
mehanata.html            цялата игра — единственият източник на истина
site/                    произведеното за хостинг (index.html, sw.js, манифест, икони)
tools/build-site.js      прави site/ от mehanata.html
tools/make-icons.js      растеризира PNG иконите за манифеста
tools/balance.js         симулация на кривата на прогресията
tests/                   Playwright набори, node tests/run.mjs
```

`mehanata.html` няма външни зависимости, няма build стъпка и няма canvas.
Всичко нужно за игра е вътре в него; `site/` само добавя нещата, които имат
смисъл при хостинг.

## Разработка

```bash
npm i -D playwright
node tests/run.mjs                # всички набори + баланс
node tools/balance.js             # само кривата
node tools/make-icons.js          # икони за манифеста
node tools/build-site.js          # сглобява site/
```

Преди комит на промяна в `mehanata.html` пусни `node tests/run.mjs`.
Наборът пази неща, които вече веднъж са се чупили — виж `tests/README.md`.

## Правила за числата

Базовите цени и приходи на първите 10 поста са фиксирани от заданието и **не
се променят**. Постове 11–15 следват същия закон: база ×15, приход ×6 на стъпка.

`tools/balance.js` вече не сверява с 0.1 — балансът е сменен нарочно в 0.3.
Проверява се попадане в целеви прозорци: първа звезда за 45–75 минути и
съдържание, което НЕ се изчерпва за вечер. Заковано е и че магазинът за
звезди не може да произведе повече от ×100 при 10¹⁸ изкарани; в 0.2 същото
даваше ×20 001 и убиваше играта за четири часа.

## Записът

Живее в `localStorage` на браузъра на всеки играч, вързан за адреса. Нищо не
се изпраща никъде — няма сървър, няма акаунти. Затова:

- различен адрес означава различен запис;
- **смяна на домейн трие всички записи**;
- Настройки → Резервно копие изнася записа като текст и го връща обратно.

Пренасянето между адреси е тествано: запис от версия 3 нататък минава през
„Възстанови от текст“ непокътнат — пари, звезди, умения, сграда и постове.

**Записи отпреди версия 3 не се пренасят както са.** Ребалансът на 0.3 смени
формулата за престиж и старите числа нямат смисъл в новата икономика. Такъв
запис почва начисто, но получава звезди, изчислени по новата формула от
изкараното за цял живот, плюс значка „Ветеран от Алфа“. Това важи и когато
старият запис се внесе през резервното копие — иначе ребалансът щеше да е
доброволен. И двата пътя са покрити от тестове.

## Пътна карта

| Версия | Какво |
|--------|-------|
| 0.1 ✅ | ядро: постове, ъпгрейди, престиж, офлайн, звук, събития — комит `64d233b` |
| 0.2 ✅ | покупка на едро, прагове на звездите, 30 постижения, PWA, Pages |
| 0.3 ✅ | ребаланс: корен трети, харчими звезди, сграда с 6 нива, постове 11–15 |
| 0.4    | персонал със заплати, крадци и неприятности |
| 0.5    | рецепти с реално време, сезони и празници |
| 0.6    | оформление за компютър: широк екран, клавиатура, мишка |
| по-късно | Android и iOS през Capacitor, чак тогава платени пакети |
